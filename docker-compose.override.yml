# Local-dev overrides (auto-merged by `docker compose`)
# Exposes db/redis to the host so you can run backend locally.
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
  redis:
    ports:
      - "127.0.0.1:6379:6379"
